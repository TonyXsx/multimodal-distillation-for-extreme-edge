## Final Test Evaluation Protocol We have finished the validation-based ablations, including teacher feature selection, bottleneck dimension selection, and student KD ablations. Now we want to stop further tuning and run the final evaluation on the held-out test set. ### Fixed settings Use the following fixed configuration for all final experiments: - Teacher representation: use the selected Qwen hidden representation from previous validation ablations. - Bottleneck dimension: use the selected best bottleneck dimension from previous validation ablations. - Student model: use the small DSResNet-SE student. - Temperature: `T = 8` - Logit KD weight: `lambda_logit = 1.0` - Feature KD weight: `lambda_feature = 1.0` - Feature loss: use the current implemented version. - Training split: train only on the original training set. - Validation split: use only for early stopping and best checkpoint selection. - Test split: use only once for final evaluation. ### Methods to evaluate Train and evaluate the following four methods under exactly the same training protocol: 1. `CE-only` 2. `Logit KD` 3. `Feature KD` 4. `Full KD` = Logit KD + Feature KD